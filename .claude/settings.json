{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",

  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(python3 -c \"import sys,json; print(json.load(sys.stdin).get('tool_input',{}).get('command',''))\" 2>/dev/null); case \"$cmd\" in *'git commit'*|*'git push'*) cat <<'MSG'\n\n🤖 vpnctl workflow reminder (CLAUDE.md → Workflow rules):\n  1. review-agent on the diff?            (Agent / general-purpose)\n  2. test-writer-agent for new public APIs? (with spec only, no impl)\n  3. After push: gh run watch <id> --exit-status to verify CI.\n\nDocs-only or <5-line hotfix? Ignore.\nMSG\n;; esac"
          }
        ]
      }
    ]
  }
}
